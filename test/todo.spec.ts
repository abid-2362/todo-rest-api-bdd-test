import "mocha";

import * as assert from "assert";
import Todo_Model from "../app/Models/Todo.model";

describe("Test command", () => {
  it("this is test", () => {
    assert(2 + 4 === 6);
  });
});
